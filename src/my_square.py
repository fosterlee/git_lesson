#!/usr/bin/env python

def my_square(z: int) -> int:
    """
    takes a value and returns the squared value.
    
    Uses the ** infix operator.
    
    :param z: int
    :return: int
    """
    return z * z


print(my_square(24))
